# Economic-Cycle
git clone ....git
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
firebase login (logout first if needed)
firebase init functions
firebase deploy --only functions